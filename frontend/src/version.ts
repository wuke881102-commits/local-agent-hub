// Single source of truth for the app version shown in the UI.
// Do NOT edit by hand on each release — run `scripts\bump_version.ps1 <new-version>`
// which keeps this file, frontend/package.json, build/installer.iss, and the backend
// (app/main.py) all in sync.
export const APP_VERSION = '5.5';
